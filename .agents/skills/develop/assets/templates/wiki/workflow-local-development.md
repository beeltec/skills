---
type: Workflow
title: "Local Development"
description: "How to set up and run this project locally."
tags: [workflows, onboarding]
status: stable
generated:
  by: human:<id>
  at: <ISO-8601 timestamp>
---

# Purpose

Get the project running locally and know which commands to use while working.

# Prerequisites

| Requirement | Version | Notes |
|-------------|---------|-------|
| <Runtime> | <version> | <How it is installed or pinned> |
| <Tool> | <version> | <Notes> |

# First-time setup

1. Clone the repository and install dependencies:

   ```bash
   <command>
   ```

2. Create local configuration:

   ```bash
   <command>
   ```

   Required values are documented in `<path>`. Never commit real secrets.

3. Prepare local services or data:

   ```bash
   <command>
   ```

4. Verify the setup:

   ```bash
   <command>
   ```

# Everyday commands

| Task | Command |
|------|---------|
| Run the application | `<command>` |
| Run tests | `<command>` |
| Run a single test | `<command>` |
| Lint | `<command>` |
| Format | `<command>` |
| Type check | `<command>` |
| Build | `<command>` |

# Local services

| Service | How it runs locally | Address |
|---------|---------------------|---------|
| <Service> | <Container, emulator, or stub> | <address> |

# Troubleshooting

| Symptom | Cause | Resolution |
|---------|-------|------------|
| <Symptom> | <Cause> | <What to do> |

# Related

* [Adding a feature](/workflows/adding-a-feature.md)
* [Testing guidelines](/guidelines/testing.md)
