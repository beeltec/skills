interface:
  display_name: "Next"
  short_description: "Find the next project workflow action"
  default_prompt: "Use $next to inspect this project and recommend the epic as one unit when its brief has stories."
