interface:
  display_name: "Ship"
  short_description: "Release done work and verify it"
  default_prompt: "Use $ship to release done leaf tickets only after any parent epic has completed integrated review and remediation."
