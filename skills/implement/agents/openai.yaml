interface:
  display_name: "Implement"
  short_description: "Implement tickets in isolated Git worktrees"
  default_prompt: "Use $implement to create ticket worktrees, run independent tickets in parallel, make conventional commits, verify each ticket, and return each to $review."
