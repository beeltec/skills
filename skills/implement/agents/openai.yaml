interface:
  display_name: "Implement"
  short_description: "Implement safely with context-aware delegation"
  default_prompt: "Use $implement to deliver the next ready item. Check session fit, delegate bounded code packets when needed, integrate them, verify the item, and prepare it for review."
