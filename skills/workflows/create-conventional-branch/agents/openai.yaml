interface:
  display_name: "Create Conventional Branch"
  short_description: "Create valid purpose-driven Git branches"
  default_prompt: "Use $create-conventional-branch to create a conventionally named Git branch for this task."
policy:
  allow_implicit_invocation: true
