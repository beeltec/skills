interface:
  display_name: "Code Review"
  short_description: "Review changes against standards and spec"
  default_prompt: "Use $code-review to review the changes since the specified fixed point against repository standards and the originating spec."
