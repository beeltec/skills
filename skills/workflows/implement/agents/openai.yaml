interface:
  display_name: "Implement"
  short_description: "Execute ready backlog work through acceptance"
  default_prompt: "Use $implement to execute this ready WORK-NNN or EPIC-NNN through claim, review, primary-branch acceptance, completion, and archival."

policy:
  allow_implicit_invocation: true
