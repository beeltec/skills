interface:
  display_name: "Implement"
  short_description: "Execute ready work with adaptive subagents"
  default_prompt: "Use $implement to execute this ready WORK-NNN or EPIC-NNN adaptively through claim, implementation, review, primary-branch acceptance, completion, and archival."

policy:
  allow_implicit_invocation: true
