interface:
  display_name: "Research Tech Stack"
  short_description: "Research current tech guidance into the wiki"
  default_prompt: "Use $research-tech-stack to research current, version-matched guidance for the affected technology stack and update the project wiki before implementation."

policy:
  allow_implicit_invocation: true
