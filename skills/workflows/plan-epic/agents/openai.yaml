interface:
  display_name: "Plan Epic"
  short_description: "Plan an Epic end-to-end to ready"
  default_prompt: "Use $plan-epic to plan this outcome into a ready Epic: Epic intake, the research decision, child work-item intake, and refinement until the Epic and all children are ready."

policy:
  allow_implicit_invocation: true
