interface:
  display_name: "Implement with Subagents"
  short_description: "Run every backlog item in a fresh subagent"
  default_prompt: "Use $implement-with-subagents to execute this EPIC-NNN or explicit WORK-NNN set with one fresh implementation subagent per item and serialized integration."

policy:
  allow_implicit_invocation: false
