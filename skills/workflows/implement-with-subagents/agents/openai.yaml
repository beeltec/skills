interface:
  display_name: "Implement with Subagents"
  short_description: "Run task plans through sequential subagents"
  default_prompt: "Use $implement-with-subagents to execute the tasks in this master task document sequentially."

policy:
  allow_implicit_invocation: false
