interface:
  display_name: "Review"
  short_description: "Review one ticket branch against standards and spec"
  default_prompt: "Use $review to verify the ticket branch base, check Standards and Spec, and loop with $implement until no P0, P1, or P2 findings remain."
