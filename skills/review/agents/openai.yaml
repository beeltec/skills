interface:
  display_name: "Review"
  short_description: "Review changes against standards and their specification"
  default_prompt: "Use $review to check the current work item against repository standards and its specification. Loop with $implement until no P0, P1, or P2 findings remain."
