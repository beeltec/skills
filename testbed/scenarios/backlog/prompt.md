I am the project owner. Use the $backlog skill for one intake-only transaction, then stop.

Intake a lightweight proposed standalone Task:

- Title: Add a truncate helper
- Outcome: the library exports `truncate(input: string, max: number): string` returning the
  input unchanged when it fits, otherwise cut to max characters ending in a single ellipsis
  character; max below 1 throws a RangeError.
- parent: none, all relationship arrays empty, no claim.
- Provenance: project-owner request in this conversation.
- Rank: first position in the global executable-work rank.

I explicitly approve exactly this record and exactly this rank placement — this message is
the owner approval for this one transaction. Do not refine it, do not research it, and do
not move it toward ready. Do not ask me any questions; if something blocks the transaction,
report and stop without writing files.
