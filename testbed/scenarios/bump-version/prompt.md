Use the $bump-version skill to bump this project's version with a patch increment, update
the changelog from the commits since the last release, and create the release commit. Do
not ask me any questions.
