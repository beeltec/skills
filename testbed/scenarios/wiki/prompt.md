I am the project owner. Use the $wiki skill for one publication, then stop.

Publish accepted current state: a concept page describing this library's module layout —
pure ESM utility functions (clamp, slugify, unique), each in its own module under `src/`,
re-exported through the `src/index.ts` barrel, with vitest tests under `tests/`. Place it
where the wiki's own organization rules say it belongs, link it from the nearest index, and
record the change in the wiki log.

This message is the owner approval for exactly this publication. Do not ask me any
questions; if the maintenance rules block it, report and stop without writing files.
