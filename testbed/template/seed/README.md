# seed-metrics

Tiny utility library: `clamp`, `slugify`, and `unique`, exported from `src/index.ts`.

- `npm run typecheck` — strict TypeScript check
- `npm run lint` — ESLint with typescript-eslint
- `npm test` — vitest
