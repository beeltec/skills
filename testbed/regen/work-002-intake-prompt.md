I am the project owner. Use the $backlog skill for one intake-only transaction, then stop.

Intake a lightweight proposed standalone Task:

- Title: Add a formatBytes helper
- Outcome: the library exports `formatBytes(size: number): string` returning a human-readable
  size with a binary unit (B, KiB, MiB, GiB) and one decimal place; negative sizes throw a
  RangeError.
- parent: none, all relationship arrays empty, no claim.
- Provenance: project-owner request in the skills-testbed fixture regeneration conversation.
- Rank: last position in the global executable-work rank.

I explicitly approve exactly this record and exactly this rank placement. Do not refine it,
do not research it, and do not move it toward ready — it must remain `status: proposed`.
Do not ask me any questions; if something blocks the transaction, report and stop.
